import { styled } from '@mui/material/styles';
import { Typography } from '@mui/material';

const CardIcon = styled(Typography)(({ theme }) => ({
    fontSize: '4rem',
    marginBottom: theme.spacing(2),
    color: '#ff4081',
  }));

export default CardIcon;